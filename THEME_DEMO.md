# 主题切换功能演示

## 🎯 功能概览

应用现在支持完整的主题切换功能，包括：
- ✅ 浅色模式
- ✅ 深色模式
- ✅ 跟随系统
- ✅ 主题持久化
- ✅ 实时切换
- ✅ 多种测试方式

## 📱 首页测试区域

### 位置
首页底部，"路由跳转测试"下方

### 包含内容
1. **当前主题显示** - 显示当前使用的主题模式
2. **三个主题按钮** - 浅色、深色、跟随系统
3. **设置页面入口** - 打开完整的主题设置页面
4. **浮动按钮** - 右下角，点击循环切换主题

### 按钮状态
- **选中状态**: 填充样式（实心按钮）
- **未选中**: 轮廓样式（空心按钮）

## 🔄 浮动按钮（右下角）

### 功能
点击循环切换主题：浅色 → 深色 → 跟随系统 → 浅色

### 图标变化
- ☀️ 浅色模式: `light_mode` 图标
- 🌙 深色模式: `dark_mode` 图标
- 🔄 跟随系统: `brightness_auto` 图标

### 提示
每次切换都会显示 Toast 提示当前模式

## ⚙️ 主题设置页面

### 进入方式
1. 首页 → "打开主题设置页面"按钮
2. 我的 → "主题设置"菜单项

### 页面功能
- **跟随系统开关**
  - 开启：自动跟随系统主题
  - 关闭：手动选择主题

- **手动选择**
  - 浅色模式（带图标）
  - 深色模式（带图标）
  - 选中项显示 ✓ 图标

## 🧪 快速测试步骤

### 1. 基础测试（30秒）
```
1. 打开应用到首页
2. 滚动到底部找到"主题切换测试"区域
3. 点击"浅色"按钮 → 观察界面变浅
4. 点击"深色"按钮 → 观察界面变深
5. 点击"跟随系统"按钮 → 观察界面跟随系统
```

### 2. 浮动按钮测试（10秒）
```
1. 连续点击右下角浮动按钮 3 次
2. 观察主题循环切换
3. 观察浮动按钮图标变化
4. 观察 Toast 提示信息
```

### 3. 持久化测试（1分钟）
```
1. 切换到深色模式
2. 完全关闭应用（从任务管理器关闭）
3. 重新打开应用
4. ✅ 应该保持深色模式
```

### 4. 设置页面测试（1分钟）
```
1. 点击"打开主题设置页面"
2. 测试跟随系统开关
3. 测试浅色/深色选择
4. 观察选中状态（✓ 图标）
5. 返回首页查看效果
```

## 💡 测试要点

### 观察界面变化
- 背景颜色
- 文字颜色
- 按钮样式
- 卡片背景
- 导航栏
- 状态栏

### 检查功能
- 切换是否流畅
- Toast 提示是否显示
- 按钮状态是否正确
- 图标是否变化
- 持久化是否生效

### 测试所有页面
- 首页
- 消息页
- 我的页
- 详情页
- 主题设置页

## 🎨 视觉效果

### 浅色模式
```
背景: 白色/浅灰
文字: 深色
按钮: 蓝色主题
卡片: 白色
```

### 深色模式
```
背景: 深灰/黑色
文字: 浅色/白色
按钮: 调整后的蓝色
卡片: 深色
```

## 📝 代码示例

### 在首页添加的代码
```dart
// 主题测试区域
Watch((context) {
  final currentMode = themeStore.themeMode.value;
  return Column(
    children: [
      Text('当前主题: ${_getThemeModeName(currentMode)}'),
      Row(
        children: [
          TDButton(text: '浅色', onTap: () => themeStore.setLightMode()),
          TDButton(text: '深色', onTap: () => themeStore.setDarkMode()),
          TDButton(text: '跟随系统', onTap: () => themeStore.setSystemMode()),
        ],
      ),
    ],
  );
})

// 浮动按钮
FloatingActionButton(
  onPressed: () {
    // 循环切换主题
    final currentMode = themeStore.themeMode.value;
    if (currentMode == AppThemeMode.light) {
      themeStore.setDarkMode();
    } else if (currentMode == AppThemeMode.dark) {
      themeStore.setSystemMode();
    } else {
      themeStore.setLightMode();
    }
  },
  child: Watch((context) {
    final mode = themeStore.themeMode.value;
    return Icon(
      mode == AppThemeMode.light ? Icons.light_mode
        : mode == AppThemeMode.dark ? Icons.dark_mode
        : Icons.brightness_auto,
    );
  }),
)
```

## ✅ 验收标准

- [x] 首页显示主题测试区域
- [x] 三个主题按钮功能正常
- [x] 浮动按钮循环切换正常
- [x] 浮动按钮图标正确显示
- [x] Toast 提示正常显示
- [x] 主题切换流畅无卡顿
- [x] 主题持久化正常
- [x] 所有页面主题一致
- [x] 设置页面功能正常

## 🎉 开始测试！

现在运行应用，滚动到首页底部，就可以看到完整的主题测试功能了！

```bash
npm run dev
```

享受主题切换的乐趣吧！🌈
