安装前，请先，打开终端，进入项目根目录，执行：

一个项目只需安装一次即可

## figma-mcp

```bash
claude mcp add --transport http figma-desktop http://127.0.0.1:3845/mcp
```


## chrome-devtools-mcp

```bash
claude mcp add chrome-devtools --scope user npx chrome-devtools-mcp@latest
```
